<!-- custom_radio_button_background.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo cuando el botón está seleccionado -->
    <item android:drawable="@drawable/selected_background" android:state_checked="true" />

    <!-- Fondo cuando el botón no está seleccionado -->
    <item android:drawable="@drawable/unselected_background" android:state_checked="false" />
</selector>
